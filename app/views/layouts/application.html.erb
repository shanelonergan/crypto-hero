<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <%= stylesheet_link_tag "application", media: "all", "data-turbolinks-track": "reload" %>
</head>
  <body>
    <div class="wrapper-1">
      <div class="nav">
        <% if @logged_in_user %>
          <li class="right-nav">
            <%=  link_to @logged_in_user.username, @logged_in_user %>
          </li>
          <li class="right-nav">
            <%= link_to "LOGOUT", logout_path %>
          </li>
          <li><%= link_to image_tag("/assets/bitcoin.png", :class => "bitcoin" ), cryptos_path %></li>
          <li><%= link_to "CRYPTOS", cryptos_path  %></li>
          <li><%= link_to "PORTFOLIO", portfolio_path(@logged_in_user) %></li>
          <li><%= link_to "EDIT PROFILE", edit_user_path(@logged_in_user) %></li>
        <% end %>
      </div>
  <% if @notification %>
    <h1> <% @notification.each do |note| %> </h1>
      <%=  note %>
    <% end %>
  <% end %>

  <% if @errors && @errors.any? %>
    <% @errors.each do |error| %>
      <%=  error %>
    <% end %>
  <% end %>


  <div class="content">
      <%= yield %>
      <footer>
        <div>Login graphic made by <a href="https://dribbble.com/DizSergei" title="Sergei Tikhonov">Sergei Tikhonov</a> from <a href="https://isometric.online/"         title="Flaticon">www.isometric.online</a></div>
        <div>Bitcoin icon made by <a href="https://www.flaticon.com/authors/becris" title="Becris">Becris</a> from <a href="https://www.flaticon.com/"         title="Flaticon">www.flaticon.com</a></div>
      </footer>
  </div>

      </div>

  </body>
</html>
